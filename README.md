# CSVtoSQLiteDB
Script to take CSV data and insert it into a SQLite database

* Using a star wars dataset for an example, but can use any csv data set
* Change LINE 40 - update database variable for your database path/name
* Code is a little messy
